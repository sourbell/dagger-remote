import { SecondsToMinsPipe } from './seconds-to-mins.pipe';

describe('SecondsToMinsPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsToMinsPipe();
    expect(pipe).toBeTruthy();
  });
});
